---
import Layout from '@layouts/Layout.astro';
// import MemberGrid from '@components/AllImageGrid.tsx'
import { getEntry, render } from "astro:content";

const about = await getEntry("pages", "home");

const { Content } = await render(about);
---
<Layout>
   <Content />
</Layout>
